node_modules/
dist/
extensions/*/.javy/
exports/
.env
.DS_Store
*.zip
orig_xlsx/
gen_xlsx/
*.xlsx
products_mock_*.csv

# Local caches/backups regenerated by CLI runs -- never meant to be committed,
# only .sync_state.json (small, deliberate timestamp marker) is tracked as
# actual pipeline state.
.price_cache.json
.snapshots/
cloudflare-worker/.snapshots/
coupon-dry-run-report.json
*.sync_state.json.bak

# Real client data must never live in this repo (product/demo template, not the
# okfish production repo) -- purged from history 2026-08-19 after being
# accidentally committed while public. Generic placeholder policy JSON files
# stay tracked; anything with real customer/product/order data does not.
customers.csv
customers.xlsx
products.csv
products_import.csv
okfish-header-extra.css
tmp_*.json
audit/

# Klientská data z onboardingu -- NIKDY necommitovat (viz clients/README.md)
clients/*/01-pozadavky/*
clients/*/02-exporty/**
!clients/_template/**/.gitkeep
!clients/README.md
